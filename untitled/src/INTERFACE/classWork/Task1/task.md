**Условие:**
1. Создайте интерфейс `FigureInterface` с константой PI = 3.1416 и методом `double area()`
2. Создайте классы `Square` (side), `Circle` (radius), `Rectangle` (side, height) — все реализуют FigureInterface
3. В main: создайте по одному экземпляру каждой фигуры, выведите площадь